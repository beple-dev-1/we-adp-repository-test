{
  "zone": "그린존",
  "artifact": "A1 기능명세서",
  "taskId": "ZERO-PAY-260915-03",
  "title": "키오스크 0원식권 복합결제 정합화 — 잔여 마일리지 우선차감 + 월렛/코인 개방",
  "writtenAt": "2026-09-15",
  "scope": "zero (페이즈 1) · wallet (페이즈 2)",
  "purpose": "`TB_QR_MNG.ZERO_AMT_SETL_YN` (0원결제여부) 플래그가 **채널마다 반대 의미로 동작**하고 있어 정합화한다.\n\n- 플래그의 확정 의미 = **「잔여한도가 0이어도 결제를 허용한다」** (`BPPG_100001_act.jsp:783` 이 플래그 N + `usableMealLmt==0` 이면 `E004 이용가능한 한도가 없습니다` 로 차단하는 것이 근거). 「한도가 남아도 식권을 0으로 만들라」가 아니다.\n- **결함 1 (키오스크)** — 승인 단계 `BPPG_100002_act.jsp:692-699` 가 잔여한도를 읽지 않고 `mealTrxAmt = 0` 으로 덮어써, 잔여 마일리지가 있어도 식권 0원 · 비플머니 전액으로 승인된다. 결제준비(`BPPG_100001:1321-1323`)는 이미 `usableMealLmt > 0 || isZeroMealTran` 라 수정 대상이 아니다.\n- **결함 2 (앱 내 결제)** — 같은 플래그를 `WALT_000005_act.jsp:315`·`COIN_000005_act.jsp:314` 는 `throw 312 \"0원 식권 결제 불가\"` 로 거부한다. 동일 구조의 `ZERO_000005_act.jsp:313-316` 은 2026-06-09(김재경) 주석 처리로 이미 해제되어 운영 중이므로, 같은 플래그가 형제 파일 사이에서 반대로 동작한다.\n\n목표는 **세 채널의 동작 결과를 일치**시키는 것이다. 채널별 금액 분할 **주체**가 서로 달라(키오스크·제로페이 = 서버가 한도로 계산 / 스마트오더 `bp_order_pay_c001` = 화면이 `MEAL_TRX_AMT`·`MNY_TRX_AMT` 를 전달) 로직 통일은 구조상 불가하며, 범위는 **동작 결과 일치**까지다.\n\n---",
  "requirements": [
    {
      "id": "R-ZERO-PAY-260915-03-01",
      "text": "키오스크(`svcCd=0001`) **혼자결제**(`tgtYn=N`) 0원식권 승인 시, 잔여 식권한도(`usableMealLmt`)를 **먼저 차감**하고 나머지를 비플머니로 결제한다 (`mealTrxAmt = usableMealLmt`, `mnyTrxAmt = trscAmt - mealTrxAmt`).",
      "trds": [
        "#1"
      ]
    },
    {
      "id": "R-ZERO-PAY-260915-03-02",
      "text": "잔여한도가 0인 경우는 **현행 동작을 유지**한다 — 식권 0원 · 비플머니 전액, 복합결제(`21`) 성립. 이 예외가 코드 조건으로 명문화되어야 한다.",
      "trds": [
        "#1"
      ]
    },
    {
      "id": "R-ZERO-PAY-260915-03-03",
      "text": "혼자결제 1회·일·월 한도검증 가드(`BPPG_100002_act.jsp:1005` 의 `!isZeroMealTran &&`)를 요구사항 01 과 정합하게 조정해, 식권이 실제로 차감되는 건은 한도검증을 지나게 한다.",
      "trds": [
        "#1"
      ]
    },
    {
      "id": "R-ZERO-PAY-260915-03-04",
      "text": "선불형 비플식권 집계 적재 가드(`BPPG_100002_act.jsp:2124` 의 `&& !isZeroMealTran`)를 조정해, 식권이 차감된 건이 `tb_prepaid_mt_sum` 정산 집계에 실리게 한다. (미조치 시 식권은 빠지는데 정산 집계에서 누락된다.)",
      "trds": [
        "#1"
      ]
    },
    {
      "id": "R-ZERO-PAY-260915-03-05",
      "text": "비플월렛 결제(`WALT_000005_act.jsp:314-317`)의 0원식권 차단을 해제하여 `ZERO_000005` 와 동일 동작이 되게 한다.",
      "trds": [
        "#2"
      ]
    },
    {
      "id": "R-ZERO-PAY-260915-03-06",
      "text": "비플코인 결제(`COIN_000005_act.jsp:313-316`)의 0원식권 차단을 해제하여 `ZERO_000005` 와 동일 동작이 되게 한다.",
      "trds": [
        "#2"
      ]
    },
    {
      "id": "R-ZERO-PAY-260915-03-07",
      "text": "범위 밖 경로의 **무변경**을 확인한다 — 함께결제(`tgtYn=Y`), 현대차 자기부담금 블록(`SysConst.ENT_HYUNDAI_ORG_ID`), `svcCd` 0002·0003·0004.",
      "trds": [
        "#1"
      ]
    }
  ],
  "requirementNote": "",
  "trds": [
    {
      "seq": 1,
      "title": "키오스크 0원식권 잔여 우선차감 교정",
      "area": "BE",
      "goal": "KIOSK(`svcCd=0001`) **혼자결제** 0원식권 승인에서 잔여 식권한도를 먼저 차감하고 나머지를 비플머니로 결제하도록 교정하고, 같은 트랜잭션의 한도검증·정산집계 가드를 함께 정합화한다",
      "covers": [
        "R-ZERO-PAY-260915-03-01",
        "R-ZERO-PAY-260915-03-02",
        "R-ZERO-PAY-260915-03-03",
        "R-ZERO-PAY-260915-03-04",
        "R-ZERO-PAY-260915-03-07"
      ]
    },
    {
      "seq": 2,
      "title": "비플월렛·비플코인 0원식권 개방",
      "area": "BE",
      "goal": "`WALT_000005_act.jsp` · `COIN_000005_act.jsp` 의 `throw 312 \"0원 식권 결제 불가\"` 차단을 해제해, 이미 해제된 `ZERO_000005_act.jsp` 와 동일 동작이 되게 한다",
      "covers": [
        "R-ZERO-PAY-260915-03-05",
        "R-ZERO-PAY-260915-03-06"
      ]
    }
  ],
  "contract": "### 6-1. DB 변경사항\n\n**없음.** IDO 신규 등록 0건, DDL(CREATE/ALTER/INDEX) 0건. JexStudio MCP 등록 대상 없음.\n\n- 단, `tb_prepaid_mt_sum` 에 **새 성격의 행이 적재되기 시작**하고(요구 04), `tb_zeropay_bppg_complex_tran` 의 **행 수가 건당 1 → 2 로 늘어난다**(요구 01). 스키마는 그대로지만 하류 집계·정산의 입력 분포가 바뀐다 — §8 위험 참조.\n\n### 6-2. 공통 모듈 변경사항\n\n**없음.** `util/`·`com/`·`cnst/`·`enums/` 무변경. `CommIDOUtil2.savePrepaidMtSum()`·`CommUtil.calcFee()` 는 호출 조건만 바뀌고 구현은 손대지 않는다.\n\n### 6-3. DTO 필드 명세\n\n본 과업은 DTO 클래스를 쓰지 않는 액션 JSP(WSVC act) 수정이다. BE 내부 계약에 해당하는 **입력 파라미터·판정 변수·출력 원장 필드**를 같은 5컬럼 형식으로 고정한다.\n\n#### (A) 페이즈 1 — BPPG_100002 승인 요청 (PG → BIZ_ZEROPAY) · 이 과업이 읽는 필드\n\n| 필드명 | 타입 | 필수 | 검증 | 의미 |\n|-------|------|:---:|------|------|\n| `svcCd` | String(4) | Y | `0001` 일 때만 본 과업 대상 | 서비스코드 (0001:KIOSK / 0002:STORE / 0003:예약 / 0004:출장) |\n| `payMeasureTp` | String(2) | Y | `21` 일 때만 0원식권 분기 진입 | 결제수단구분 (06:식권단독 / 10:머니단독 / 21:식권+머니 / 23:식권+급여공제) |\n| `trscAmt` | String(숫자) | Y | `> 0`, `Long.parseLong` 가능 | 총 결제금액 |\n| `trscDt` | String(8) | Y | `YYYYMMDD` | 거래일자 |\n\n#### (B) 페이즈 1 — 서버 내부 판정 계약 (`qrInfo` 유래) · **이 과업의 핵심**\n\n| 필드명 | 타입 | 필수 | 검증 | 의미 |\n|-------|------|:---:|------|------|\n| `qrInfo.ZERO_AMT_SETL_YN` | char(1) | N (기본 `N`) | `Y`/`N` | **0원결제여부** = 「잔여한도 0이어도 결제 허용」. `Y` + `payMeasureTp=21` + `svcCd=0001` → `isZeroMealTran = true` (`:565-567`) |\n| `usableMealLmt` | String(숫자) | Y (기본 `\"0\"`) | `>= 0` | 한도금액·일·월·1회 한도 4종의 **최소값** (`:412-446`). **요구 01 의 식권 차감액 상한** |\n| `tgtYn` | char(1) | N (기본 `N`) | `Y`/`N` | 함께결제 여부. **`N` 만 이번 교정 대상** |\n| `qrInfo.CALC_YN` | char(1) | N | `Y`/`N` | 정산여부 — 요구 04 의 `savePrepaidMtSum` 적재 조건 중 하나 |\n| `qrInfo.CPMT_YN` | char(1) | N | `Y`/`N` | 자기부담금 여부 (현대차 전용). **범위 밖 — 무변경 확인만** |\n\n#### (C) 페이즈 1 — 계산 결과 계약 (원장 적재 전 확정값)\n\n| 필드명 | 타입 | 필수 | 검증 | 의미 |\n|-------|------|:---:|------|------|\n| `mealTrxAmt` | long | Y | `0 <= mealTrxAmt <= usableMealLmt` | 비플식권 결제금액. 교정 후 `= usableMealLmt`(잔여한도 > 0 일 때) |\n| `mnyTrxAmt` | long | Y | `>= 0`, **`mealTrxAmt + mnyTrxAmt == trscAmt`** | 비플머니 결제금액 |\n| `salyTrxAmt` | long | Y | `== 0` (21 경로) | 급여공제 결제금액 — 21 경로에서는 0 |\n| `isComplexTran` | boolean | Y | `mnyTrxAmt > 0` 이면 `true` | 복합결제 여부. 수수료·VAT 분해(`:1063-`)·원장 `COMPLEX_YN` 의 입력 |\n| `isMealTran` / `isMnyTran` | boolean | Y | 각 금액 > 0 과 정합 | 원장·HUB 통지 분기 플래그 |\n\n> **불변식** — `mealTrxAmt + mnyTrxAmt + salyTrxAmt == Long.parseLong(trscAmt)` 는 기존 검증(`:861-863` 등)이 이미 강제한다. 교정 후에도 이 식이 깨지지 않아야 한다.\n\n#### (D) 페이즈 2 — WALT/COIN_000005 입력 계약\n\n| 필드명 | 타입 | 필수 | 검증 | 의미 |\n|-------|------|:---:|------|------|\n| `COMPLEX_YN` | String(1) | N (기본 `N`) | `Y`/`N` | 앱이 보내는 복합결제 요청 여부. `isComplexTran` 의 1차 조건 (`WALT:130-135` / `COIN:129-134`) |\n| `PAY_TYPE` | String(1) | N (WALT 기본 `A`) | `A`/`P`/`C` | 결제수단 (A:계좌 P:포인트 C:코인). **COIN 은 `\"C\"` 하드코딩** (`COIN:206`) |\n| `TGT_YN` | String(1) | N | `Y`/`N`/빈값 | 함께결제 여부. **빈값이면 `isComplexTran` 이 `false`** 가 된다 |\n| `MNY_CHRG_AMT` | String(숫자) | N (기본 `0`) | `>= 0` | 비플머니 자동충전 금액 — `isAutoCharge` 입력 |\n| `AMT` | String(숫자) | Y | `!= \"\"`, `!= \"0\"` | 결제 총액 |\n\n#### (E) 페이즈 2 — 서버 내부 판정 계약 (`userSelected` 유래)\n\n| 필드명 | 타입 | 필수 | 검증 | 의미 |\n|-------|------|:---:|------|------|\n| `userSelected.ZERO_AMT_SETL_YN` | char(1) | N | `Y`/`N` | **현재 `Y` 이면 `throw 312` — 이 과업이 해제하는 지점** (`WALT:315` / `COIN:314`) |\n| `COMPLEX_USE_YN` | String(1) | N (기본 `N`) | `Y`/`N` | `userSelected.MIX_SETL_USE_YN` 사본. **`Y` 가 아니면 복합 분해 전체가 건너뛰어진다** (`WALT:518` 등 40여 곳) |\n| `complexMealAmt` | long | Y (복합 시) | `<= 각 한도` | 복합결제 시 식권 몫. 한도 3종으로 클램프 (`WALT:518-545`) |\n\n---",
  "security": "- 개인정보·인증정보 신규 노출 **없음** — 회원코드(`MEMB_CD`)·계좌번호는 기존 로그 라인 그대로이며 신규 로그를 추가하지 않는다.\n- 금액 계산 변경이므로 **금액 조작 가능 입력이 늘지 않는지** 확인이 필요하다. 교정 후 식권 차감액의 상한은 서버가 산출한 `usableMealLmt` 이며 **클라이언트 입력이 아니다** — 클라이언트가 식권 몫을 지정할 수 있는 경로는 생기지 않는다.\n- 요구 03(한도검증 가드 조정)은 **검증을 여는 방향**이다 — 종전에 검증을 건너뛰던 건이 1회·일·월 한도검증을 지나게 되므로 보안·통제 관점에서 완화가 아니라 강화다.\n- 시크릿·DB 접속정보 취급 없음. JexStudio MCP 호출 없음.\n\n---",
  "source": {
    "plan": "plan/dev_plan.md",
    "phases": 2
  }
}